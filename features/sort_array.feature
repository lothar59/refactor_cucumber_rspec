Feature: sort an array
  
  Will sort non nil comparable objects and return them in an array 

  Scenario: Sort an array of object
    Given an array of objects
    Then an array will be returned, sorted
